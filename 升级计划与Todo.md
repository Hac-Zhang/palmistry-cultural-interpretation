# 国风 AI 手相解析升级计划

## 当前目标

在现有“五纹 + 双语 + 流年 + 多视图”基础上，逐步扩展掌相观察维度，同时保持：

- 先描述图像中可见的形态，再做传统文化解读。
- 看不清的纹理明确标记为“隐而不显”，不反向捏造。
- 不输出医疗、心理诊断、寿命或确定性宿命结论。
- 模型原文不使用程序补句，避免重复和空泛内容。
- API 输出保持稳定，前端对缺失字段安全降级。

## 阶段一：规则与资料整理（当前优先级：高）

- [x] 固化地纹、人纹、天纹、玉柱纹、家风纹的字段映射。
- [x] 记录《麻衣相法》及现代手相入门资料作为术语来源。
- [ ] 新增太阳线、火星线、水星线的观察卡片。
- [ ] 新增木星丘、土星丘、太阳丘、水星丘、金星丘、月丘等掌丘卡片。
- [ ] 新增星纹、十字纹、三角纹、岛纹、鱼纹等特殊纹的“可见性优先”规则。
- [ ] 为每条规则记录：观察位置、可描述形态、文化解释方向、禁止结论、来源版本。
- [ ] 更新规则版本号，并为每次规则变更保留变更记录。

## 阶段二：多视图采集与图像能力（优先级：高）

- [x] 保留完整概览图、宽边界掌心图、增强图三种视图。
- [ ] 增加掌底/腕纹局部视图。
- [ ] 增加小指侧/家风纹局部视图。
- [ ] 增加掌丘区域视图，避免裁剪掉边缘结构。
- [ ] 增加左右手双图上传与配对分析。
- [ ] 对每个视图记录质量分数、可见区域和遮挡提示。
- [ ] 用测试图片验证不同手型、光线、角度和浅纹场景。

## 阶段三：后端模型与数据契约（优先级：高）

- [x] 使用 OpenAI 兼容多模态接口，并支持 `.env.local` 配置。
- [x] 禁止继承失效系统代理，确保模型请求可直连配置的服务端点。
- [x] 输出 observations、analysis、timeline、master_pan_ci 和固定免责声明。
- [x] 移除统一补句；模型短文本原样保留，缺失字段才使用独立默认文案。
- [ ] 将新增纹理纳入统一 JSON Schema，并保持旧字段兼容。
- [ ] 增加模型响应的字段级日志（只记录长度、缺失字段和错误码，不记录密钥、原图或完整提示词）。
- [ ] 对超时、空响应、Markdown 包裹 JSON、字段缺失分别增加重试和错误测试。
- [ ] 评估结构化输出能力，必要时增加 JSON Schema response format。

## 阶段四：前端体验（优先级：中）

- [x] 国风长卷轴结果页。
- [x] 古文/白话切换。
- [x] 五纹卡片和折叠式“观相明细”。
- [ ] 增加“更多纹理”分组，按可见性显示，不可见时给出清晰说明。
- [ ] 增加左右手对照视图和差异摘要。
- [ ] 增加报告生成状态、失败原因和重新上传建议。
- [ ] 对超长文本、空字段和旧版响应做视觉回归测试。

## 阶段五：质量、安全与发布（优先级：中）

- [x] 图片 MIME、大小、解码和掌心检测校验。
- [x] 固定服务端免责声明，避免交给模型生成。
- [x] API Key 仅从服务端环境变量读取，`.env.local` 不提交。
- [ ] 增加请求限流、耗时统计和脱敏结构化日志。
- [ ] 增加临时图像数据清理策略。
- [ ] 完成清晰掌纹、模糊、遮挡、非手掌、左右手和多视图回归测试。
- [ ] 发布前执行 `pytest`、接口冒烟测试和前端手工验收。
- [ ] 使用独立发布分支或版本标签部署，保留回滚点。

## 里程碑与验收标准

### M1：扩展规则库

太阳线、火星线、水星线和主要掌丘具备可追溯规则；不可见纹理不会被捏造。

### M2：多视图增强

单手多视图可稳定覆盖掌底、掌侧、腕部和掌丘；左右手上传不会破坏旧接口。

### M3：完整体验发布

模型响应结构稳定，前端可安全渲染；测试通过，敏感配置未进入 Git，服务可从健康接口确认可用。

## 开发约定

每完成一个阶段，先运行测试，再更新本文件的复选框和变更记录，最后以独立提交推送到 GitHub。任何规则解释都只能作为传统文化体验，不包装为科学验证结论。

