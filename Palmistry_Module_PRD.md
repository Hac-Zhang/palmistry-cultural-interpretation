# AI 手相文化解读模块（MVP）产品与系统规范

## 1. 产品定位

本模块是一个基于掌心照片的传统手相文化娱乐解读工具。它只描述图像中可能观察到的掌线形态，并提供经过产品化改写的传统文化解释，不宣称科学准确性。

禁止将结果用于医疗、心理诊断、寿命预测、招聘、保险、投资、婚姻或其他重要人生决策。结果页必须显示免责声明：

> 本报告仅供传统文化与娱乐参考，不构成医学、心理、法律、财务或人生决策建议。图片质量不足时，系统可能无法可靠识别掌线。

## 2. MVP 范围

### 支持

- Web 端上传单张 JPG、JPEG 或 PNG 掌心照片。
- 检测一只手、判断左右手（无法判断时返回 unknown）。
- 识别三大主线：生命线、智慧线、感情线。
- 输出观察描述、观察证据、置信度和传统文化解释。
- 对模糊、遮挡、非手掌或掌心不可见的图片返回可理解的失败原因。

### 暂不支持

- 事业线、太阳线、婚姻线等细副线。
- 双手对照、时间线推断、年龄分段预测。
- 疾病、寿命、心理状态或确定性事件预测。
- 自动保存原图和以原图训练模型。

## 3. 系统工作流

1. 前端校验扩展名、MIME、文件大小并生成本地预览。
2. FastAPI 接收 `multipart/form-data`，再次校验 MIME、文件大小和像素尺寸。
3. 图像预处理：检测手部/关键点、估算掌心裁剪框、计算质量分数，并生成原图、裁剪图和可选 CLAHE 增强图。
4. 质量分数低于阈值时停止推理，返回 `IMAGE_UNCLEAR` 及拍摄建议。
5. 模型适配层向配置的多模态模型发送图片、规则摘要和输出 Schema。
6. 后端解析并用 Pydantic/JSON Schema 校验；失败时有限次数重试，仍失败返回 `MODEL_OUTPUT_INVALID`。
7. 前端渲染观察、传统文化解释、置信度、局限性和免责声明。

## 4. 图像输入要求

- 支持格式：`image/jpeg`、`image/png`。
- 默认大小上限：10 MB；默认最短边：640 px；最大边：4096 px。
- 建议掌心占画面 60% 以上，掌心朝向镜头，五指自然分开，避免强反光、阴影、滤镜和遮挡。
- 预处理不覆盖原图。灰度化和 CLAHE 仅作为辅助视图；模型可同时参考原图与增强图。
- MediaPipe Hands 用于关键点和手部存在性判断；若需要实例分割，必须提供专门训练的检测/分割模型，不能假定通用 YOLO 权重具备该能力。

## 5. 规则范围与资料来源

规则库采用“传统术语 + 产品化解释”双层结构：

- 传统文化参考：《麻衣相法》，用于掌线、掌丘及传统分类的历史术语来源。不同版本存在篇章和归属差异，文档不把其内容视为科学结论。
- 现代入门参考：陈鼎龙《中国手相学·掌相入门》或同类现代手相教材，用于核对常用掌线名称、位置和形态描述。仅记录摘要和术语映射，不复制受版权保护的整段内容。
- 规则优先级：先写可见观察，再写传统文化解释；观察不确定时不得生成确定性解释。

### 三大主线（MVP）

| 主线 | 图像观察范围 | 允许的文化解释方向 |
|---|---|---|
| 生命线 | 起点、绕拇指根部的走向、连续性、深浅、分叉/链状/岛状 | 活力、节奏、恢复力等象征性描述；不得推断寿命或疾病 |
| 智慧线 | 起点、横向或下垂走向、长度、连续性、末端分叉 | 思考风格、专注与想象力等象征性描述；不得诊断能力或人格障碍 |
| 感情线 | 小指侧起点、向食指/中指方向走向、弧度、连续性、分叉/链状 | 情感表达、关系偏好等象征性描述；不得预测婚姻或心理疾病 |

“长短=寿命”“断裂=疾病”“过短=性格冷淡”等确定性规则禁止直接使用。所有解释必须使用“传统手相通常将其象征为”“可能呈现”“仅作文化参考”等限定语。

## 6. 输出契约

模型必须返回符合 Schema 的 JSON，核心字段如下：

```json
{
  "technical_metrics": {"image_quality": "High|Medium|Low", "confidence_score": 0},
  "observations": {
    "earth_line": "",
    "human_line": "",
    "heaven_line": "",
    "jade_pillar_line": "",
    "family_ethos_line": ""
  },
  "analysis": {
    "foundation": "",
    "wisdom": "",
    "karma": "",
    "career": "",
    "marriage": ""
  },
  "master_pan_ci": "潜龙在渊，静待时飞"
}
```

`technical_metrics` 只供前端做视觉效果，不直接展示给用户。免责声明由服务端固定追加，在报告底部统一展示；模型正文不重复输出免责声明。

## 7. 非功能要求

- API Key 只存在服务端环境变量，不进入前端或日志。
- 默认不持久化原图；临时文件在请求完成或超时后删除。
- 记录请求 ID、耗时、错误类型和模型版本，不记录原始图片或完整提示词中的敏感数据。
- 单请求模型调用设置超时、有限重试和速率限制。
- 记录模型提供商、模型名、Schema 版本和规则库版本，便于回溯。

## 8. MVP 验收指标

- 合法图片校验覆盖率 100%。
- 正常样例 JSON Schema 校验通过率 ≥ 99%。
- 非手掌、严重模糊和遮挡图片能稳定返回质量错误。
- 模型不得输出疾病诊断、寿命预测或确定性宿命结论。
- 统计平均响应时间、失败率、低质量拒答率和单次模型成本。
