# AI 手相文化解读模块开发文档

## 1. 推荐目录

```text
backend/
  app/main.py                 # FastAPI 入口
  api/routes.py               # HTTP 路由
  schemas/report.py           # Pydantic 请求/响应模型
  services/image_quality.py   # 文件校验、质量评分、手掌检测
  services/preprocess.py      # 裁剪、灰度、CLAHE 辅助视图
  services/model_adapter.py   # 多模态模型适配层
  rules/palmistry_rules.yaml  # 规则摘要与版本
  tests/
frontend/
  src/components/PalmUpload.tsx
```

## 2. HTTP 接口

### `POST /api/v1/palmistry/analyze`

请求类型为 `multipart/form-data`，字段：

- `image`：必填，JPG/JPEG/PNG，最大 10 MB。
- `rule_version`：可选，默认当前规则版本。

成功响应 `200` 返回 PRD 中定义的报告 JSON，并附加 `request_id`、`model` 和 `rule_version`。

失败响应统一为：

```json
{
  "request_id": "uuid",
  "error": {"code": "IMAGE_UNCLEAR", "message": "无法清晰识别掌心，请重新拍摄。", "details": []}
}
```

错误码：

- `UNSUPPORTED_MEDIA_TYPE`：不是支持的图片格式。
- `FILE_TOO_LARGE`：超过大小限制。
- `IMAGE_UNCLEAR`：模糊、遮挡、过曝、掌心过小或没有检测到手掌。
- `MODEL_TIMEOUT`：模型调用超时。
- `MODEL_OUTPUT_INVALID`：模型输出无法通过 Schema 校验。
- `INTERNAL_ERROR`：未分类服务错误。

## 3. 图像处理约定

1. 校验真实 MIME 和图片解码结果，不能只信任文件扩展名。
2. 读取 EXIF 方向并统一旋转。
3. 使用 MediaPipe Hands 做手部存在性和关键点检查，估算掌心区域；检测失败直接返回 `IMAGE_UNCLEAR`。
4. 生成三类模型视图：完整掌心概览图、保留较宽边界的掌心裁剪图、CLAHE 增强图。概览图保留掌底、掌侧和腕部结构；局部图用于观察细纹；CLAHE 参数为 `clipLimit=2.0`、`tileGridSize=(8, 8)`，但不能替代质量评分。
5. 裁剪边界默认增加约 22% 边距，避免玉柱纹起于掌底或家风纹位于小指侧时被裁掉。模型 Prompt 明确要求先看概览图，再用局部图和增强图交叉确认。
6. 质量评分至少综合清晰度、亮度、掌心占比、遮挡和关键点完整度。

## 4. Pydantic 输出模型

实现时应建立与 PRD JSON 一致的模型：

- `technical_metrics.image_quality: High | Medium | Low`
- `technical_metrics.confidence_score: int`，范围 0–100，仅供 UI 效果使用
- `observations.earth_line/human_line/heaven_line/jade_pillar_line/family_ethos_line: str`，默认在“观相明细”折叠区展示
- `analysis.foundation/wisdom/karma/career/marriage: str`，作为五张国风核心卡片展示
- `master_pan_ci: str`，作为印章式报告标题和核心判词
- `disclaimer` 使用服务端固定文本，不能完全交给模型生成。

模型输出经 Schema 校验后才可返回前端。必要时最多重试一次，并在重试提示中指出具体校验错误。

## 5. 规则库格式

`backend/rules/palmistry_rules.yaml` 只保存结构化摘要，例如：

```yaml
version: "1.0.0"
positioning: "traditional_culture_entertainment"
lines:
  life_line:
    aliases: ["生命线"]
    observation_features: ["起点", "走向", "连续性", "深浅", "分叉", "链状", "岛状"]
    interpretation_templates:
      - "传统手相通常将连续、清晰的线条象征为节奏较稳定；仅供文化参考。"
    forbidden_claims: ["寿命", "疾病诊断", "心血管预测"]
```

每条规则必须同时包含观察字段、传统解释模板和禁止结论。规则版本随响应返回。

### 三条主线规则摘要（产品化写法）

| 线路 | 可记录的观察特征 | 传统文化解释模板 | 禁止输出 |
|---|---|---|---|
| 生命线 | 起点是否清晰、弧度、连续性、深浅、分叉、链状或岛状 | “传统手相通常把较清晰、连续的形态象征为精力与生活节奏较稳定；仅供文化参考。” | 寿命、疾病、体质诊断、心血管风险 |
| 智慧线 | 与生命线起点关系、横向/下垂走向、长度、连续性、末端分叉 | “传统手相通常把偏平直的形态与分析取向联系起来，把明显下垂的形态与想象力取向联系起来；不代表能力测评。” | 智商、人格障碍、职业适配结论 |
| 感情线 | 小指侧起点、弧度、终点位置、连续性、分叉、链状 | “传统手相通常将弧度和分叉作为情感表达风格的象征性线索；不代表真实关系结果。” | 婚姻成败、出轨预测、心理疾病、确定性情感事件 |

规则执行顺序固定为：`图像可见性判断 → 客观形态描述 → 置信度 → 传统象征性解释 → 固定免责声明`。任何无法确认的形态都必须保留为“不确定”，不能通过书籍规则反向补图。

## 6. System Prompt 模板

```text
你是一位深谙《麻衣相法》《柳庄相法》等中国传统相学经典的相学宗师。
只根据提供的掌心图像描述可见的生命线、智慧线、感情线，不要臆测不可见内容。

工作要求：
1. 先输出客观观察：位置、走向、连续性、深浅、分叉、链状或岛状等。
2. 将生命线称地纹、智慧线称人纹、感情线称天纹；补充玉柱纹（事业线）和家风纹（婚姻线）；用古雅、温和、积极的语言给出根基禀赋、心智悟性、情志因缘、事业基业和姻缘际会的娱乐性解读。
3. 禁止疾病诊断、寿命预测、心理诊断、确定性事件预测和重大决策建议；不要反复输出免责声明。
4. 如果图片模糊、遮挡或角度不合适，用“朦胧、重组、蜕变”等不确定表达，不要编造细节。
5. 只返回新的三才纹 JSON Schema，不输出 Markdown、思维链或额外说明。
```

免责声明由服务端追加或覆盖，避免模型遗漏。

## 7. 环境变量与安全

```text
MODEL_PROVIDER=openai
VISION_MODEL=<configured-model-name>
OPENAI_API_KEY=<server-only-secret>
MAX_UPLOAD_BYTES=10485760
REQUEST_TIMEOUT_SECONDS=90
TEMP_FILE_TTL_SECONDS=300
RULE_VERSION=1.0.0
```

生产环境必须启用 HTTPS、请求限流、结构化日志脱敏和临时文件清理；不得将 API Key、原图或 base64 图片写入日志。

## 8. 测试清单

- 合法 JPG/PNG、错误扩展名、伪造 MIME、超大文件。
- 清晰左手、清晰右手、旋转照片、低分辨率、过曝、反光、遮挡、非手掌图片。
- 三条主线清晰、部分不可见、完全无法确认。
- 模型返回合法 JSON、缺字段、错误类型、Markdown 包裹 JSON、超时和空响应。
- 断言：低质量图片不进入模型；非法输出不返回前端；免责声明始终存在；禁止结论不会出现在报告中。

## 9. 资料记录

- 《麻衣相法》：传统相学术语和分类的文化来源；版本、作者归属和年代存在争议。
- 陈鼎龙：《中国手相学·掌相入门》：现代中文入门资料，用于术语和图示对照。
- 资料只用于建立规则摘要和术语映射；实现不得把传统说法包装成经过科学验证的事实。

## 10. 模型选择与是否需要训练

当前 `.env.local` 配置的服务端点可返回 `gpt-5.5`、`gpt-5.4`、`grok-4.6` 等模型。MVP 默认使用 `gpt-5.5`，原因是它已在当前 OpenAI 兼容端点中可用，并适合图像理解和结构化输出。模型名通过 `AI_MODEL` 配置，不写死在业务逻辑中。

第一阶段不需要训练或微调模型。原因是：

- 需求是通用掌心图像理解和传统术语解释，视觉基础模型已经具备基本图像能力；
- 当前没有经过专家标注的掌纹数据集，训练会把民俗规则和图像事实混在一起，反而增加不可控风险；
- 规则需要可追溯、可审查和可快速修改，放在版本化 YAML 与 Prompt 中更合适。

后续只有在收集到经授权、去标识化、由手相领域人员标注的图像与观察数据后，才评估视觉检测模型或 LoRA/微调。微调也只能改善“掌线可见性与形态标注”，不能把传统手相解释变成科学预测。
